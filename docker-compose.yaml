services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: auth_db
    volumes:
      - pg-data:/var/lib/postgresql
    ports:
      - "5434:5432"

volumes:
  pg-data:
